<?xml version="1.0" encoding="UTF-8"?>
<hazelcast
        xsi:schemaLocation="http://www.hazelcast.com/schema/config http://www.hazelcast.com/schema/config/hazelcast-config-3.5.xsd"
        xmlns="http://www.hazelcast.com/schema/config"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <properties>
        <property name="hazelcast.logging.type">${hazelcast.logging.type}</property>
    </properties>
    <group>
        <name>${group.name}</name>
        <password>${group.password}</password>
    </group>
    <management-center enabled="false">${mancenter.url}</management-center>
    <network>
        <join>
            <tcp-ip enabled="true">
                <member>192.168.1.10</member>
            </tcp-ip>
        </join>
        <interfaces>
            <interface>192.168.1.10</interface>
        </interfaces>
    </network>

</hazelcast>
